{
  "name": "@inq/db",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/client.ts",
    "./repositories/*": "./src/repositories/*.ts"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "db:ensure": "node scripts/ensure-sqlite-database.mjs",
    "prisma": "prisma",
    "test": "vitest run tests",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "@types/node": "^25.9.4",
    "dotenv": "^16.6.1",
    "prisma": "^7.8.0",
    "typescript": "^5.9.3",
    "vitest": "^4.0.15"
  },
  "dependencies": {
    "@inq/shared": "workspace:*",
    "@prisma/adapter-better-sqlite3": "7.8.0",
    "@prisma/client": "^7.8.0"
  }
}
